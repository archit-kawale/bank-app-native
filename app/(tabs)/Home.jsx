const { Text, SafeAreaView } = require("react-native")

const Home = () =>{
    return (
        <SafeAreaView>
            <Text style={{color:'#fff'}}>Home</Text>
        </SafeAreaView>
    );
}

export default Home;